server:
  port: 8080
grpc:
  server:
    port: 8091
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/BankRush?useSSL=false&createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true
    username: root
    password: Root#234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL57Dialect
    hibernate:
      ddl-auto: update
